name: Weekly Progress Report Auto-Commit

on:
  schedule:
    - cron: '29 18 * * 6'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install reportlab matplotlib

      - name: Generate weekly report
        run: |
          python DevOpsAssignment_Group11/generate_report.py weekly

      - name: Move report to weekly_reports
        run: |
          mkdir -p weekly_reports
          mv *_Weekly_Progress_Report_*.pdf weekly_reports/

      - name: Commit and push report
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add weekly_reports/
          git commit -m "Auto-generate weekly progress report" || echo "No changes to commit"
          git push